# Pinned to the versions the project was built and tested with.

# Big-data stack
pyspark==4.1.2
pyarrow==24.0.0

# Ingestion
requests==2.34.2
gtfs-realtime-bindings==2.0.0
protobuf==7.35.1
PyYAML==6.0.3

# Analysis + geo
pandas==3.0.4
numpy==2.5.0
geopandas==1.1.4
shapely==2.1.2
pyogrio==0.13.0

# Visualisation
matplotlib==3.11.0
folium==0.20.0
streamlit==1.58.0
pydeck==0.9.2          # the dashboard's map layer; imported directly

# Notebooks + tests
jupyter==1.1.1
nbconvert==7.17.1
nbclient==0.11.0
pytest==9.1.1
